# daily-lab

Small, self-contained web/UI experiments — one new one at a time. Each experiment lives in its own folder under `experiments/` and gets linked from `index.html`.

Live: https://maretyui.github.io/daily-lab/
